# Mean-Variance-Standard-Deviation-Calculator

This project is part of the freeCodeCamp **Data Analysis with Python** certification.

## 📌 Description
A Python function that uses **NumPy** to calculate the **mean, variance, standard deviation, maximum, minimum, and sum** of a 3×3 matrix.  
It outputs results for **columns**, **rows**, and the **entire matrix**.

## 🛠 Technologies Used
- Python
- NumPy

## 📂 Files
- `mean_var_std.py`: Contains the `calculate()` function (main code)
- `main.py`: Used for local testing (optional)
- `test_module.py`: Unit tests (provided by freeCodeCamp)

## ▶ How to Run

1. Make sure you have Python installed.
2. Run the `main.py` file to test the function:

```bash
python3 main.py
